// Deck definitions for the blockBasics/blockGames/blockMath/blockScience
// tutorial categories (Phase 3, issue #680). Stub — populated by #680 so
// that category can be developed without touching index.jsx or the
// other tutorial categories (issue #932).

const decks = {};

export default decks;
