#!/usr/bin/env node
'use strict';
const { main } = require('../src/pw-check');
main(process.argv.slice(2))
    .then((code) => process.exit(code || 0))
    .catch((e) => { process.stderr.write(`pw-check: ${e.message}\n`); process.exit(1); });
